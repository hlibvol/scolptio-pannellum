import { SelectedRoomHeaderPipe } from './selected-room-header.pipe';

describe('SelectedRoomHeaderPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectedRoomHeaderPipe();
    expect(pipe).toBeTruthy();
  });
});
