import { QuestionnairesCompletedPipe } from './questionnaires-completed.pipe';

describe('QuestionnairesCompletedPipe', () => {
  it('create an instance', () => {
    const pipe = new QuestionnairesCompletedPipe();
    expect(pipe).toBeTruthy();
  });
});
